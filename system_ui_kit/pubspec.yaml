name: system_ui_kit
description: >-
  Project-agnostic Flutter helpers for synchronizing status bar and navigation
  bar colors with custom app bars and top surfaces.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
