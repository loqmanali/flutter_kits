name: flutter_kits_tool
description: Repo tooling for the flutter_kits monorepo. Not published, not a kit.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.8.0

dependencies:
  pub_semver: ^2.1.4
  yaml: ^3.1.2
