name: navigation_kit
description: Self-contained, project-agnostic navigation module for Flutter — GoRouter shells, auth/role/guest/feature-flag guards, deep links, observers, transitions.
version: 1.0.0
publish_to: "none"
repository: https://example.com/navigation_kit

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
